import type { Greeting } from "./db";

export const renderGreeting = (greeting: Greeting) => {
  console.log("RENDERING GREETING", greeting);

  // TODO:
  // 1. Create a .star file that contains the greeting
  // 2. Render the .star file to a .webp
  // 3. Upload the .webp to the Tidbyt
};
